import { LayoutTraversable } from "../types";

export function composeLayoutTraversable(layout: LayoutTraversable) {
  /*
   * Composition
   * 1) Create required meshes
   * 2) Sequence meshes into batches
   * */
  // -- Roads
  // //////////////////// ADD TAGS TO
  // const pathsTriangulationOuterMap = map(
  //   (path) => getPathTriangulation(path, 14),
  //   layout.pathMap
  // );
  // const pathsTriangulationInnerMap = map(
  //   (path) => getPathTriangulation(path, 10),
  //   layout.pathMap
  // );
}
